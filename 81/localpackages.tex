\usepackage{subcaption}
\usepackage{caption}
\usepackage{langsci/styles/jambox}
\usepackage{multirow}
\usepackage{makecell}
\usepackage{tabularx}
\usepackage{overpic}
\usepackage{tikz}
\usepackage{placeins} %für flotumgebung von graphiken \FloatBarrier
\usepackage{graphicx}
\usepackage{langsci/styles/langsci-gb4e}
\usepackage{hhline}
%\usepackage{tipa} %there might be some bits left where it is used, so just to be sure [OS]
\usepackage{colortbl}
\let\clipbox\relax % PSTricks and Adjustbox compatibility
\usepackage{adjustbox}
\usepackage{array}
\usepackage{dcolumn}

\usetikzlibrary{fit}
\usetikzlibrary{positioning}
\usetikzlibrary{backgrounds}
\usetikzlibrary{arrows}
\usetikzlibrary{decorations.markings}
\usetikzlibrary{calc}
\tikzset{%
	vecOpenArrowRed/.style={
		thick,
		red,
		decoration={
			markings,
			mark=at position 1 with {\arrow[semithick,red]{open triangle 60}}
		},
		double distance=1.4pt,
		shorten >= 5.5pt,
		preaction = {
			decorate
		},
		postaction = {
			draw,
			line width=1.4pt,
			white,
			shorten >= 4.5pt
		}%
	},
	vecArrowOlive/.style={
		thick,
		red,
		decoration={
			markings,
			mark=at position 1 with {\arrow[semithick,olive!40!]{triangle 60}}
		},
		double distance=1.4pt,
		shorten >= 5.5pt,
		preaction = {
			decorate
		},
		postaction = {
			draw,
			line width=1.4pt,
			white,
			shorten >= 4.5pt
		}%	
	},
	innerOlive/.style={
		semithick,
		olive!40!,
		line width=1.4pt,
		shorten >= 4.5pt
	}%
}

\usepackage{graphicx}

\usepackage{subcaption}
\usepackage{todonotes}